# Portal theme screenshots

Evidence images referenced from PR comments on `schuecl/nexus-rag`. This is an
orphan branch on a fork holding nothing but PNGs -- it is never proposed
upstream, so review evidence can be viewed inline without putting binaries in
the repo history that matters.

Each render is a static Jinja render of `upload.html` plus headless Chrome, at a
1440px and a 500px viewport, per portal theme.
